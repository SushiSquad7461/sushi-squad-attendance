{
  "name": "sushi-squad-attendance-pages",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "tailwind": "npx tailwindcss -i ./src/styles.css -o ./dist/styles.css --watch",
    "dev": "vite",
    "build": "npx tailwindcss -i ./src/styles.css -o ./dist/styles.css && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  }
}
